export class Player
{
    band
    marge
    tour

    constructor()
    {
        this.initializePlayer()
    }

    //TODO: move to JSON, read from there
    initializePlayer()
    {
        this.band = 
        {
            john: bandMate,
            mike: bandMate,
            stanli: bandMate,
            zack: bandMate
        }

        //initialize band

        this.marge =
        {
            enum: this.marge.speeds =
            {
               0: 0, 1: 1, 2: 2, 3: 3, 4: 4
            },
            Road: this.marge.road

        }

        this.tour = 
        {
            
        }
    }
}

class bandMate
{
    
}